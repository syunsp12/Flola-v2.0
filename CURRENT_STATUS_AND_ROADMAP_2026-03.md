# Flola 現状完成度評価と改修ロードマップ（2026-03）

## 1. 現状の完成度（要約）

### 総合

- **完成度: 82/100（運用可能、ただし運用品質/構造改善フェーズが必要）**

### 領域別評価

- **セキュリティ/認証: 88/100**
  - 管理APIは Bearer 認証 + 互換モード（`ALLOW_LEGACY_QUERY_AUTH`）を実装済み。
  - 管理系 Server Action は `assertAdmin()` が導入済み。
- **実行信頼性: 80/100**
  - 給与PDF解析は `PAYROLL_PYTHON_CMD -> python3 -> python` フォールバックでクロスプラットフォーム化済み。
  - 主要エラーコード分類済み。
- **コード品質: 92/100**
  - `web` の ESLint は **errors=0 / warnings=0** 到達。
  - 型注釈強化、`any` の大幅削減を完了。
- **保守性: 70/100**
  - `app/actions.ts` が依然として巨大で責務分離余地が大きい。
- **運用品質: 66/100**
  - ログ基盤はあるが、通知自動化・SLO定義・AI精度評価ループは未完成。

---

## 2. 既に完了している主な改修

1. 管理APIの認証方式を Bearer ベースに統一（互換モード付き）。
2. 管理系Actionに `assertAdmin()` を適用し、非管理者拒否を明確化。
3. 給与PDF解析のクロスプラットフォーム対応（実行コマンド解決、エラー分類）。
4. 型強化 + UI側の安全化（主要ページ/コンポーネント全体）。
5. Lint バーンダウン完了（`errors=0`, `warnings=0`）。

---

## 3. 未完了・今後改善が必要な作業（優先度順）

## P1（次スプリントで着手）

### P1-1. `app/actions.ts` の責務分離

- **目的**: 変更影響範囲を限定し、レビュー容易性を向上。
- **提案分割**
  - `app/actions/transactions.ts`
  - `app/actions/assets.ts`
  - `app/actions/admin.ts`
  - `app/actions/payroll.ts`
  - `app/actions/index.ts`（再エクスポート）
- **Done**
  - 1ファイル1責務を満たす。
  - 既存呼び出し元の import 互換維持。

### P1-2. 認証互換モードの段階的廃止計画

- **目的**: `ALLOW_LEGACY_QUERY_AUTH` を恒久運用しない。
- **内容**
  - 期間を区切って query auth アクセス数を計測。
  - 廃止日を定め、最終的に Bearer のみへ固定。
- **Done**
  - 廃止日合意 + 移行アナウンス + 無効化完了。

## P2（4〜8週で実施）

### P2-1. 重複検知ロジック強化

- **目的**: webhook/取込時の重複混入を削減。
- **内容**
  - 正規化列（例: `normalized_description`）導入。
  - 可能な連携は `source_event_id` でユニーク判定。
- **Done**
  - 手動重複修正件数の月次減少。

### P2-2. 運用監視強化（通知・SLO）

- **目的**: 障害の早期検知。
- **内容**
  - `system_logs`/`job_status` から失敗イベントを Slack 通知。
  - 連続失敗閾値でエスカレーション。
- **Done**
  - 「画面を見るまで気づかない」を解消。

### P2-3. AI分類の品質評価ループ

- **目的**: 推論品質を定量管理。
- **内容**
  - 採用率・修正率・誤分類上位カテゴリを定点観測。
  - 月次でカテゴリ辞書/プロンプトを更新。
- **Done**
  - 施策ごとの改善効果をトラッキング可能。

---

## 4. 実行ロードマップ（提案）

### Sprint A（1〜2週）

- `actions.ts` 分割（最優先）
- 互換認証アクセスのログ計測追加
- 受け入れ: `npm run build` + 主要 API 回帰確認

### Sprint B（3〜4週）

- 重複検知ロジック強化（正規化 + 判定キー改善）
- 監視通知（Slack）最小導入
- 受け入れ: 取り込み重複率/KPI を計測開始

### Sprint C（5〜8週）

- AI品質KPI運用
- 互換認証廃止の最終実施
- Runbook 更新（障害一次対応手順含む）

---

## 5. 直近の受け入れ基準（次のPR群向け）

- 静的解析: `cd web && npx eslint . -f json` が `errors=0`
- ビルド: `cd web && npm run build` 成功
- セキュリティ回帰:
  - Bearer 正常系 200
  - ヘッダ無し 401
  - 不正トークン 403
- 管理権限回帰:
  - admin 成功
  - 非admin `Forbidden`
